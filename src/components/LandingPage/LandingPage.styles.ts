import styled from '@emotion/styled';

export const StyledLandingPage = styled.div`
  text-align: center;
`;

export const LandingPageTitle = styled.h1`
  font-size: 30px;
  padding: 15px 0;
`;

export const CenterSvg = styled.span`
  display: inline-flex;
  align-items: center;
  justify-content: center;
  
  & > svg {
    margin-left: 5px;
  }
`